% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy_logistic_regression.R
\name{easy_logistic_regression}
\alias{easy_logistic_regression}
\title{Run a Logistic Regression with Automated Narrative Reporting}
\usage{
easy_logistic_regression(formula, data, alpha = 0.05, conf_level = 0.95)
}
\arguments{
\item{formula}{A \code{formula} object or a character string formula (e.g.,
\code{"am ~ mpg + wt"}). The outcome must be binary.}

\item{data}{A data frame containing the variables referenced in \code{formula}.}

\item{alpha}{Significance threshold used in narrative generation. Default \code{0.05}.}

\item{conf_level}{Confidence level for odds-ratio intervals. Default \code{0.95}.}
}
\value{
An object of class \code{"easystat_result"} with coefficient, odds-ratio,
  model-fit, raw \code{glm}, and narrative components.
}
\description{
Executes a binary logistic regression using \code{stats::glm()} with
\code{family = binomial}, extracts coefficients, odds ratios, approximate
confidence intervals, and model-fit metrics, then generates a plain-language
narrative via the Narrative Generator Module.
}
\examples{
result <- easy_logistic_regression(am ~ mpg + wt, data = mtcars)
print(result)

}
